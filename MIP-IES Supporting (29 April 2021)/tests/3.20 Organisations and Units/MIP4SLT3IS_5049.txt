Test description

Test IDMIP4SLT3IS_5049Test TitleCreate and update Organisation or Unit with CommentText.Execution Priority1ObjectiveBSO validation. Test whether the Producer/Provider is able to create the correct MIP data for all the operational elements (defined by IDT Schema Documentation > IESUnits.xsd > CombatEngineerUnit, as an example Unit or Organisation, it might be any other) and the Consumers are able to correctly understand (and display) it. Focused on the Unit's CommentText Metadata element.ScenarioThe Producer creates message elements for an Organisation or Unit with at least a CommentText Metadata element, along with all its mandatory Elements. The Consumers receive the data and process them.EnvironmentInternet or Co-located. Exchange Pattern: R/R, P/S.Participation2 or more.MTRSN/APre-test ConditionsThe Producer/Provider has created the 'Ground Picture > Forces' topic. The Consumers have subscribed to this topic.Test InputsN/A.ConclusionThis test is considered a success if the unit is created and all elements are processed and interpreted correctly on all Nations' C2IS.Test OutputsN/A.TraceabilityREQ_INF_0002.

Test Procedure

StepActionExpected ResultSrcDst1. The Producer creates an example Unit or Organisation with name XXX_CT_049 and fills ALL its mandatory elements with values, as well as its CommentText Metadata Element.

Notices:
XXX in name of the object is meant (in principle) to be replaced by the 3 letter code of the country name.
Example for the name of the unit: ITA_CT_049 ~~
Consumers receive and process (and display) the example Unit or Organisation created and all elements that the Producer created. ~~


122. The Producer updates the previous example Unit or Organisation with HostilityStatusCode 'Suspect' and writes a generic comment (using long_txt with at least 25 characters) about the event in the CommentText Metadata Element.
Keeping the former values for the rest of elements. ~~Consumers receive and process the example Unit or Organisation and all elements that the Producer updated. ~~

12
Configuration
ItemValueCommentEventGeneration10 --> Combine steps to one event in the MTMT, generate new MTMT events on every source - destination change.
1 --> Every step will be added to the MTMT as a separate event.




2



					          


1

